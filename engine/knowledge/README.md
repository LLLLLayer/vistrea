# Knowledge Engine

Builds Runtime Graph and Deep Wiki semantics, links, backlinks, derived views, and knowledge queries through `data/api`.

It owns knowledge behavior, not SQLite schemas, physical artifact paths, or Hub transport.
