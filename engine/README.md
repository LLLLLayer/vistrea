# Host Engine

Reusable application and domain capabilities for Workspace lifecycle, connection, device automation, exploration, design review and tuning, Deep Wiki knowledge, validation, versioning, and synchronization. The Engine does not depend on product UI or concrete storage implementations.

See [Host Engine use cases](../docs/interfaces/ENGINE_API.md).
