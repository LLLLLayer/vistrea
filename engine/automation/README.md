# Device Automation

Manages WDA and UIAutomator lifecycle, real device actions, and system UI interaction behind a shared interface for tap, type, swipe, back, launch, screenshot, and related operations.

See [Device Automation API](../../docs/interfaces/AUTOMATION_API.md).
