# Contract Tests

Verify consistent interpretation of protocol schemas, fixtures, versions, Data API contracts, and platform extensions across Swift, Kotlin, Host, Data, and Hub implementations.

The executable suites cover strict JSON, manifest-owned fixtures, `DataUnitOfWork` model coverage, aggregate semantic guards, Workspace bootstrap, and deep Snapshot trees. Run them through `pnpm test:contract`.
