# Contract Tests

Verify consistent interpretation of protocol schemas, fixtures, versions, Data API contracts, and platform extensions across Swift, Kotlin, Host, Data, and Hub implementations.

The first executable suite is `protocol-fixtures.test.mjs`. Run it through `pnpm test:contract`.
