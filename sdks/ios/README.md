# Vistrea iOS SDK

iOS Runtime SDK and in-app Inspector. UIKit is the first adapter; SwiftUI semantics should be added through an explicit separate adapter.

After toolchain selection, keep Core, UIKit Adapter, SwiftUI Adapter, Inspector, protected Design Tuning, Transport, and Tests as separate modules.
