# Runtime SDKs

In-process white-box observation for native applications. SDKs capture and describe real runtime state; Host automation performs real user interactions. Debug/Internal builds may additionally enable reversible allowlisted visual-property overrides.
