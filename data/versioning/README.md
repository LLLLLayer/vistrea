# Versioning Store

Stores Vistrea commit graphs, parents, refs, tags, baselines, and diff metadata.

The semantics are Git-like, but runtime objects are not stored in Git. Observations remain immutable; graph and Deep Wiki states are materialized from selected commits or refs.
