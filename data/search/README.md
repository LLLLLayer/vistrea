# Search Index

Search for screens, routes, text, components, transitions, issues, and design references. The first implementation may use SQLite FTS. Every index must be rebuildable from authoritative metadata and objects.
